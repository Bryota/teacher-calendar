<h1>ユーザー一覧</h1>
<ul>
    <% if @users.size > 0 %>
        <% @users.each do |user| %>
            <li><%= link_to user.name, user_path(user.id) %></li>
        <% end %>
    <% end %>
</ul>
<%= link_to '新規追加', new_user_path, class: 'btn btn__reserve' %>
